// React Native Components
import { StyleSheet } from 'react-native';

export const featuredJobsListStyles = StyleSheet.create({
    featuredJobsListContainer: {
        display: 'flex', 
        flexDirection: 'row',
        marginTop: 10,
    },
});